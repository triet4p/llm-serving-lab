[project]
name = "llm-serving-lab"
version = "0.1.0"
description = "A compact, clone-and-run playground for demonstrating how LLMs are served and consumed through different inference backends."
requires-python = ">=3.11"

[dependency-groups]
dev = [
    "pytest>=8.0",
]
