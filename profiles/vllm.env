# vLLM backend profile
#
# vLLM is the main optimized serving backend. The OpenAI-compatible endpoint
# is exposed by `vllm serve`. Confirm the exact OPENAI_BASE_URL when the
# runner is implemented in Sprint 3.
OPENAI_BASE_URL=http://localhost:8000/v1
OPENAI_API_KEY=demo
MODEL_NAME=Qwen/Qwen3-8B
