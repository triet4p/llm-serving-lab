# Ollama backend profile
#
# Ollama uses short model tags (unlike the HF-style names used by the
# baseline and vLLM profiles). Confirm the exact OPENAI_BASE_URL when the
# runner is implemented in Sprint 4.
OPENAI_BASE_URL=http://localhost:11434/v1
OPENAI_API_KEY=demo
MODEL_NAME=qwen3:8b
