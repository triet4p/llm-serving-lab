{
  "name": "default",
  "description": "The original single-prompt benchmark (one-sentence explanation prompt, 1024 max tokens). Equivalent to running without --dataset.",
  "requests": [
    {
      "label": "default",
      "prompt": "Explain in one sentence what an LLM serving engine does.",
      "max_tokens": 1024
    }
  ]
}
